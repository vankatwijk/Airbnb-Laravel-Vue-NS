import { GridLayout } from 'tns-core-modules/ui/layouts/grid-layout';
export interface NativeChatConfig {
    bot: Bot;
    channel: Channel;
    user?: User;
    session?: Session;
    googleApiKey?: string;
    locale?: string;
    placeholder?: string;
    submitLocationText?: string;
    defaultLocation?: Location;
    css?: string[];
    showDebugConsole?: boolean;
    chatServerInstance?: string;
}
export interface Bot {
    id: string;
    avatarUrl?: string;
    name?: string;
}
export interface Channel {
    token: string;
    id: string;
}
export interface User {
    id?: string;
    avatarUrl?: string;
    name?: string;
    token?: string;
}
export interface Session {
    clear?: boolean;
    context?: object;
    userMessage?: string;
}
export interface Location {
    latitude: number;
    longitude: number;
}
export declare class NativeChat extends GridLayout {
    private _webView;
    private _config;
    private webChatConfig;
    private _configChangeListener;
    config: NativeChatConfig;
    constructor();
    private webViewLoaded;
    private configPropertyChange;
    private updateNativeChatConfig;
    private handleConfigChanges;
    private attachPropertyChange;
    private removeConfigChangesListeners;
    private removePropertyChange;
    private isObservable;
}
