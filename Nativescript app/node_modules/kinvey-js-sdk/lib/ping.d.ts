export interface PingOptions {
    timeout?: number;
}
export interface PingResponse {
    version: string;
    kinvey: string;
    appName: string;
    environmentName: string;
}
export declare function ping(options?: PingOptions): Promise<PingResponse>;
